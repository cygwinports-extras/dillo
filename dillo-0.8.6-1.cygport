DESCRIPTION="Dillo compact web browser"
HOMEPAGE="http://www.dillo.org/"
SRC_URI="http://www.dillo.org/download/${P}.tar.bz2"

CYGCONF_ARGS="--disable-glibtest --disable-gtktest --disable-dlgui"

src_install() {
	cd ${B}
	cyginstall

	for dpi in $(find ${D}/usr/lib/dillo -name '*.exe')
	do
		strip ${dpi}
		mv ${dpi} ${dpi%.exe}
	done

	make_etc_defaults /etc/dillorc
}
